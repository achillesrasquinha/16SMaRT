pcr.seqs(fasta={{ silva_seed }}, start={{ silva_seed_start }}, end={{ silva_seed_end }}, keepdots=FALSE, processors={{ processors }})

unique.seqs(fasta={{ merged_fasta }})
count.seqs(name=current, group={{ merged_group }})
align.seqs(fasta=current, reference={{ silva_seed_pcr }}, processors={{ processors }})
summary.seqs(fasta=current, processors={{ processors }})
screen.seqs(fasta=current, count=current, maxhomop={{ maxhomop }}, processors={{ processors }})
unique.seqs(fasta=current, count=current)

pre.cluster(fasta=current, count=current, processors={{ processors }})
chimera.vsearch(fasta=current, reference={{ silva_gold }})
remove.seqs(fasta=current, accnos=current)
classify.seqs(fasta=current, count=current, reference={{ silva_seed_pcr }}, taxonomy={{ silva_seed_tax }}, cutoff=80, processors={{ processors }})