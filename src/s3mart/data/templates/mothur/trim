{% if layout == "paired" %}
make.file(inputdir={{ inputdir }}, prefix={{ prefix }})
make.contigs(file=current, processors={{ processors }})
{% endif %}
trim.seqs(fasta=current, qaverage={{ qaverage }}, processors={{ processors }})
screen.seqs(fasta=current, group=current, maxambig={{ maxambig }}, maxhomop={{ maxhomop }}, minlength={{ min_length }}, maxlength={{ max_length }}, processors={{ processors }})
summary.seqs(fasta=current)